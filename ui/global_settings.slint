export global GlobalSettings {
    in property <bool> widgets-enabled: true;

    in property <int> current-page: 0;

    in property <float> balance: 110.23;

    in-out property <length> FontSizeLarge: 15pt;
    in-out property <length> FontSizeDefault: 12pt;
}
